import { render } from '@testing-library/react';
import { ConfigureSystemPage } from 'src/pages';

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders configure system page', () => {
  const { container } = render(ConfigureSystemPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});
