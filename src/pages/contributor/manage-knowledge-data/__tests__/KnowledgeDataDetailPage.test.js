import { render } from '@testing-library/react';
import { KnowledgeDataDetailPage } from 'src/pages';

it('Test renders knowledge data detail page', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the intent field', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the intent fullname field', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the reference list', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the tokenized word', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test cancel of the raw data', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test remove the subject data', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the reference list modal', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add new reference', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add new synonym set', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test render of the error Alert', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test comment feature', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test generated Question', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test review feature of the question', () => {
  const { container } = render(KnowledgeDataDetailPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});
