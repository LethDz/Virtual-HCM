import { render } from '@testing-library/react';
import { KnowledgeDataCreateFormPage } from 'src/pages';

it('Renders knowledge data create form page', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test Intent Data field render', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test Intent Data field render', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add reference button clickable', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test raw data field render', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test Tokenizing data in raw data field', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test Cancel Tokenized Data in raw data field', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test tokenized word is filled in the dropdown box', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add tokenized word to subject', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add tokenized word to verb of the subject', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test question label select box', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test corresponding answer field', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add question', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add base response', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('Test add add Synonyms', () => {
  const { container } = render(KnowledgeDataCreateFormPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});
