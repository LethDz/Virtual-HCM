import { render } from '@testing-library/react';
import { ChatHistoryListPage } from 'src/pages';

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Chat history list page', () => {
  const { container } = render(ChatHistoryListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

