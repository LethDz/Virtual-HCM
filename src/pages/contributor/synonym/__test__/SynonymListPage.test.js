import { render } from '@testing-library/react';
import { SynonymListPage } from 'src/pages';

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders synonym list page', () => {
  const { container } = render(SynonymListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});
