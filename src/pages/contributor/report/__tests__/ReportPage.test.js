import { render } from '@testing-library/react';
import { ReportPage } from 'src/pages';

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report page', () => {
  const { container } = render(ReportPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});


