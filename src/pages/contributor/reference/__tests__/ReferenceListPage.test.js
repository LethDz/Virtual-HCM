import { render } from '@testing-library/react';
import { ReferenceListPage } from 'src/pages';

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders reference list page', () => {
  const { container } = render(ReferenceListPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

