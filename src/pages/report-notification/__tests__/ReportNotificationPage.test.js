import { render } from '@testing-library/react';
import { ReportNotificationPage } from 'src/pages';

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Report Notification page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ReportNotificationPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

