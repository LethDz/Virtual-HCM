import { render } from '@testing-library/react';
import { ForgotPasswordPage } from 'src/pages';

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Forgot Password page', () => {
  const { container } = render(ForgotPasswordPage());
  expect(container).toMatchInlineSnapshot(`<div />`);
});
