import { render } from '@testing-library/react';
import { ResetPasswordPage } from 'src/pages';

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});

it('renders Reset Password page', () => {
  const props = {
    location: {
      search: 'test',
    },
  };
  const { container } = render(ResetPasswordPage(props));
  expect(container).toMatchInlineSnapshot(`<div />`);
});
